<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="com.example.todaymovie.util.TMDbExtKt"/>
        <variable
            name="tv"
            type="com.example.todaymovie.domain.model.TV" />

        <variable
            name="clickListener"
            type="android.view.View.OnClickListener" />
    </data>
    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintDimensionRatio="8:4"
        android:onClick="@{clickListener}"
        tools:context=".ui.movie.banner.MovieBannerFragment">
        <ImageView
            android:scaleType="fitXY"
            loadImage="@{TMDbExtKt.getPosterPath(tv.backdropPath)}"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:ignore="ContentDescription" />
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_gravity="bottom"
            android:layout_marginBottom="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <RatingBar
                android:id="@+id/ratingBar_movie"
                android:theme="@style/RatingBar2"
                style="?android:attr/ratingBarStyleIndicator"
                android:layout_marginTop="4dp"
                android:layout_marginHorizontal="@dimen/margin_standard"
                android:stepSize="0.1"
                android:numStars="5"
                android:isIndicator="true"
                ratingAverage="@{tv.voteAverage}"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHorizontal_bias="0"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
            <TextView
                android:id="@+id/tv_rating_number"
                ratingCount="@{tv.voteCount}"
                app:layout_constraintTop_toTopOf="@id/ratingBar_movie"
                app:layout_constraintBottom_toBottomOf="@id/ratingBar_movie"
                app:layout_constraintStart_toEndOf="@id/ratingBar_movie"
                app:layout_constraintHorizontal_bias="0"
                app:layout_constraintEnd_toEndOf="parent"
                android:shadowColor="@color/text_normal_color_primary_opposite"
                android:shadowRadius="1"
                android:textColor="@color/text_normal_color_primary"
                android:textSize="@dimen/text_normal_small"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
            <TextView
                android:layout_marginHorizontal="@dimen/margin_standard"
                android:maxLines="2"
                android:ellipsize="end"
                app:layout_constraintTop_toBottomOf="@id/ratingBar_movie"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:text="@{tv.name}"
                android:textStyle="bold"
                android:shadowColor="@color/text_normal_color_primary_opposite"
                android:shadowRadius="1"
                android:textColor="@color/text_normal_color_primary"
                android:textSize="@dimen/text_normal_medium"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>
        </androidx.constraintlayout.widget.ConstraintLayout>
    </FrameLayout>
</layout>
